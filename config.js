var cards = [
    {
        "title":"初音ミク",
        "comment":"一緒に歌おう。"
    }
];

var select = cards[0]; //cards[Math.floor(Math.random() * 2)];

var config = {
    "sitename":"St@rtline",
    "title":select.title,
    "comment":select.comment
};